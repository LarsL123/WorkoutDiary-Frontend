/*
    Default config for development can be added here.
    All env variables has to start with the "WORKOUT_" prefix to work in production.
    Production config are added at runtime, see nginx/start.sh
*/
var WORKOUT_BACKEND_URI = "http://localhost:4000/api";//"http://192.168.1.248:4000/api";
